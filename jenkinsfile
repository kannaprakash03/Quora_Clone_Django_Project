pipeline {
    agent any

    triggers {
        githubPush()
    }

    stages {
        stages('Checkout') {
            steps {
                //Checkout the repository
                checkout scm
            }
        }
    }
}
